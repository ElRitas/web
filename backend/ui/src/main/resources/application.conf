ktor = {
    deployment {
        port = 8080

    }
    application {
        modules = [ org.example.Main.module ]
    }
}

database {
    url = "jdbc:postgresql://postgres:5432/andrejtrohan"
    driver = "org.postgresql.Driver"
    user = "postgres"
    password = "123"
}

jwt {
    secret = "Michael_Mamaev"
    issuer = "your-app-issuer"
    audience = "your-app-audience"
    realm = "Your App Realm"
}